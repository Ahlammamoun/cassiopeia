{% extends 'back/back_base.html.twig' %}




{% block body %}
 <h2 class="text-center text-dark fst-normal">Student</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ student.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ student.lastname }}</td>
            </tr>
            <tr>
                <th>Average</th>
                <td>{{ student.average }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_student_index') }}" class="btn btn-dark w-100 fst-normal">back to list</a>

    <a href="{{ path('app_back_student_edit', {'id': student.id}) }}" class="btn btn-success w-100 fst-normal">edit</a>

    {{ include('back/student/_delete_form.html.twig') }}
{% endblock %}
