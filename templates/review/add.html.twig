{% extends 'base.html.twig' %}

{% block title %}
<div class="col-lg-6 mx-auto text-center ">
     <p class="lead mb-4 text-muted fst-normal fw-bold">cassiopeia</p>
</div>

<div class="my-2 py-2 text-center">    
    <img src="{{ asset('assets/favicon.png') }}" height="100" alt="logo" class="logo">
    <h1 class="display-6 fw-bold">Enseignements, Exellence, quality </h1>
</div>

    <h2 class="display-6 fw-bold fst-normal text-center">Votre commentaire sur le cursus</h2>
    <h2 class="display-6 fw-bold fst-normal text-center">{{ circuit.title}}</h2>

{% endblock %}

{% block body %}

{{ form_start(formulaire, {novalidate: 'novalidate'}) }}

    {{ form_widget(formulaire) }}
    <button class ="btn btn-dark" type="submit">Ajouter</button>

{{ form_end(formulaire) }}
{% endblock %}
