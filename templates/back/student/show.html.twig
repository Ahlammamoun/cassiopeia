{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>Student</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ student.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ student.lastname }}</td>
            </tr>
            <tr>
                <th>Average</th>
                <td>{{ student.average }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_student_index') }}">back to list</a>

    <a href="{{ path('app_back_student_edit', {'id': student.id}) }}">edit</a>

    {{ include('back/student/_delete_form.html.twig') }}
{% endblock %}
