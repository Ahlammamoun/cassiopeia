{% extends 'base.html.twig' %}

{% block title %}
<div class="col-lg-6 mx-auto text-center ">
     <p class="lead mb-4 text-muted fst-normal fw-bold">cassiopeia</p>
</div>
    {% endblock %}

{% block body %}
<div class="my-2 py-2 text-center">
    <img src="{{ asset('assets/favicon.png') }}" height="100" alt="logo" class="logo">
    <h1 class="display-6 fw-bold">Enseignements, Exellence, quality </h1>
  
</div>

<div class="my-0 py-0 text-center">
<div class="col-lg-9 mx-auto">
    <h2 class="display-7 fw-bold">Les Professeurs</h2>




<p>
    {% for teacher in teachers %}
        <span class="d-flex mb-4">{{ teacher.firstname }} {{ teacher.lastname }}</span><br>


    {% endfor %}

 {% for circuit in teachers.circuit %}
        <span class="d-flex mb-4">{{ circuit.id }}</span><br>


    {% endfor %}

</p>
{% endblock %}
