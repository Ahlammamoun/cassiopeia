
{% extends 'back/back_base.html.twig' %}


{% block body %}
    <h2 class="text-center text-dark fst-normal">Circuit</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ circuit.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ circuit.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ circuit.description }}</td>
            </tr>
            {#<tr>
                <th>Course</th>
                <td>{{ circuit.course }}</td>
            </tr>#}
                <tr>
                <th>Teacher</th>
                <td>{{ circuit.teacher }}</td>
            </tr>
               {#<<tr>
                <th>Student</th>
                <td>{{ circuit.student }}</td>
            </tr>#}
            <tr>
                <th>Duration</th>
                <td>{{ circuit.duration }}</td>
            </tr>
            <tr>
                <th>Difficulties</th>
                <td>{{ circuit.difficulties }}</td>
            </tr>
            <tr>
                <th>Success</th>
                <td>{{ circuit.success }}</td>
            </tr>
            <tr>
                <th>Diplomas</th>
                <td>{{ circuit.diplomas }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ circuit.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_circuit_index') }}">back to list</a>

    <a href="{{ path('app_back_circuit_edit', {'id': circuit.id}) }}">edit</a>

    {{ include('back/circuit/_delete_form.html.twig') }}
{% endblock %}
